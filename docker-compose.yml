services:
  shorts-video:
    build: .
    ports:
      - "8283:8283"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-2.5-flash}
      - NODE_ENV=production
      - PORT=8283
    volumes:
      - ./data/output:/app/data/output
